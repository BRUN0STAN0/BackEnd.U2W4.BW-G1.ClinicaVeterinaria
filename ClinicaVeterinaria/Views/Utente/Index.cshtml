@model IEnumerable<ClinicaVeterinaria.Models.Utente>

@{
    ViewBag.Title = "Index";
}

<h2>AREA AMMINISTRAZIONE</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Psw)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @Html.HiddenFor(modelItem => item.Psw)
            ********
        </td>
        <td>
            @Html.ActionLink("Modifica", "Edit", new { id=item.ID_Utente }) |
            @Html.ActionLink("Dettagli", "Details", new { id=item.ID_Utente }) |
            @Html.ActionLink("Elimina", "Delete", new { id=item.ID_Utente })
        </td>
    </tr>
}

</table>
