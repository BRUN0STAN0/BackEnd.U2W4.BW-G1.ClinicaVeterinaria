@model IEnumerable<ClinicaVeterinaria.Models.TipologiaAnimale>

@{
    ViewBag.Title = "Index";
}

<h2>Lista Tipologia Animali</h2>

<p>
    @Html.ActionLink("Visualizza la lista animali", "Index", "Animale")
</p>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TipologiaAnimale }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TipologiaAnimale }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TipologiaAnimale })
        </td>
    </tr>
}

</table>
